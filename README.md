# integration-health-monitor
an n8n-driven integration that reconciles orders between two systems (Sales app vs ERP), auto-flags mismatches, sends alerts, and feeds a dashboard tracking sync failures and resolution times.
